name: Save Webpage to Single HTML

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'כתובת ה-URL לשמירה'
        required: true
        type: string

jobs:
  save-page:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install SingleFile CLI
        run: npm install -g single-file-cli

      - name: Download page and embed assets
        run: |
          single-file "${{ github.event.inputs.url }}" output.html --browser-executable-path=""

      - name: Upload HTML Artifact
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-webpage
          path: output.html
